// Type reference for listings — data is served from the API (/api/listings).
// See: artifacts/iatreia/src/lib/listings.ts for fetch helpers and the DbListing type.

export type ListingCategory = "spaces" | "equipment" | "jobs" | "supplies" | "services";
